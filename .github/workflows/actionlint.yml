name: 🚨 actionlint

on:
  push:
    branches:
      - long_lived/**
      - main
      - release/**
    tags:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  actionlint:
    uses: ./.github/workflows/reflow-actionlint.yml
